import "package:flutter/material.dart";

class pageviews extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Pageviews",style: TextStyle(fontSize: 20.0),),),
      
    );
  }
}